#  ____   ____ __  __ 
# |  _ \ / ___|  \/  |
# | | | | |   | |\/| | This compose file was generated by DCM: https://github.com/ajnart/dcm
# | |_| | |___| |  | |
# |____/ \____|_|  |_|
#
services:

  # Jellyfin: A free software media system that puts you in control of managing and streaming your media. It is an alternative to the proprietary Emby and Plex, with no premium features behind a paywall.
  jellyfin:
    image: ghcr.io/hotio/jellyfin:latest
    container_name: ${CONTAINER_PREFIX}jellyfin
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - UMASK=${UMASK}
    volumes:
      - ${CONFIG_PATH}/jellyfin:/config
      - ./opt/data/media:/data/media
      - /mnt/storage/VIdeo:/data/movies  # Host path : Container path
    ports:
      - 8096:8096
    restart: ${RESTART_POLICY}
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s